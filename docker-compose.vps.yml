services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://mabuha-phase3-api.proxy.itcollege.ee}
    restart: unless-stopped
    ports:
      - "${RANKFORGE_CLIENT_PORT:-8090}:80"
